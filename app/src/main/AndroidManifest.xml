<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.wh2.reduxLogin">

    <application
        android:name="com.wh2.reduxLogin.Application"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">

        <activity
            android:name="com.wh2.reduxLogin.ui.activities.SplashActivity"
            android:launchMode="singleInstance">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <activity android:name="com.wh2.reduxLogin.ui.activities.AuthActivity" android:launchMode="singleInstance" />

        <activity android:name="com.wh2.reduxLogin.ui.activities.HomeActivity" />

    </application>

</manifest>